'use strict'; // import Doc from '../../model/doc';

// test('two plus two is four', () => {
//   let doc = new Doc({filename: '../files/test.md'});
//   doc.test().toBe(2);
// })

var sum = require('../../model/doc');

test('adds 1 + 2 to equal 3', function () {
  expect(sum(1, 2)).toBe(3);
});